/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh '''
                    cleanWs()
                    checkout scm
                '''
            }
        }
        stage('Unit Test') {
            steps {
                sh '''
                    cd maven-docker-pipeline
                    mvn test
                '''
            }
        }
        stage('Package') {
            steps {
                sh '''
                    cd maven-docker-pipeline
                    mvn package
                '''
            }
        }
    }
}
